# Runtime dependencies — also used by Vercel to build the serverless function.
# For local dev + tests:  pip install -e ".[web,dev]"  (adds pytest, reportlab)
phonenumbers>=8.13
pycountry>=22.3
pdfplumber>=0.10
python-docx>=1.1
flask>=3.0
